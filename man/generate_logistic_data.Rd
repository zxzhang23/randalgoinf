% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pra_logistic.R
\name{generate_logistic_data}
\alias{generate_logistic_data}
\title{Generate Logistic Regression Data}
\usage{
generate_logistic_data(theta, d = length(theta))
}
\arguments{
\item{theta}{True coefficient vector (parameter of the logistic model)}

\item{d}{Dimension of features (default = length(theta))}
}
\value{
List containing:
\item{X}{Design vector (length d)}
\item{y}{Binary response (scalar)}
}
\description{
Generates (X, y) pairs for logistic regression where y follows Bernoulli distribution
with probability determined by the logistic function of X'*theta.
}
\examples{
# Generate logistic regression data point
set.seed(123)
theta <- c(1, -0.5, 0.3)
data <- generate_logistic_data(theta)
print(c(data$X, data$y))

}
\keyword{internal} 