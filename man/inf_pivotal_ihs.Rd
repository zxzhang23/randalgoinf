% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivotal_inference.R
\name{inf_pivotal_ihs}
\alias{inf_pivotal_ihs}
\title{Pivotal Inference for Iterative Sketching}
\usage{
inf_pivotal_ihs(
  X,
  y,
  c,
  m,
  iterations = 5,
  alpha = 0.1,
  num_simulations = 100,
  refresh_sketch = TRUE
)
}
\arguments{
\item{X}{Design matrix (n x p)}

\item{y}{Response vector (n x 1)}

\item{c}{Linear combination vector (p x 1)}

\item{m}{Sketch size per iteration}

\item{iterations}{Number of iterations}

\item{alpha}{Significance level (default = 0.10)}

\item{num_simulations}{Number of simulations for quantile estimation (default = 500)}

\item{refresh_sketch}{Whether to refresh sketching matrix each iteration (default = TRUE)}
}
\value{
List containing:
\item{point_estimates}{Point estimates from each iteration}
\item{confidence_intervals}{Matrix of confidence intervals for each iteration}
\item{lower_bounds}{Lower confidence bounds for each iteration}
\item{upper_bounds}{Upper confidence bounds for each iteration}
}
\description{
Performs pivotal inference for iterative sketch-and-solve estimators using
Gaussian Orthogonal Ensemble (GOE) simulation.
}
\examples{
# Generate sample data
set.seed(123)
n <- 5000; p <- 10; m <- 500
X <- matrix(rnorm(n * p), n, p)
true_beta <- rnorm(p)
y <- X \%*\% true_beta + rnorm(n, sd = 0.1)
c_vec <- rep(1, p)

# Pivotal inference for iterative sketching
inference <- inf_pivotal_ihs(X, y, c_vec, m = 500, iterations = 5,
                             num_simulations = 100)
cat("Final point estimate:", inference$point_estimates[5], "\\n")
cat("Final 90\% CI: [", inference$lower_bounds[5], ",",
    inference$upper_bounds[5], "]\\n")

} 